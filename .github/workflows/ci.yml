name: Deepiri CI - deepiri-external-bridge-service

on:
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
      - main
      - dev

permissions:
  contents: read
  packages: read 

jobs:
  ci:
    name: Test, Build & Docker
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          path: platform-services/backend/deepiri-external-bridge-service

      - name: Checkout shared-utils
        uses: actions/checkout@v4
        with:
          repository: Team-Deepiri/deepiri-shared-utils
          path: platform-services/shared/deepiri-shared-utils

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: platform-services/backend/deepiri-external-bridge-service/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: platform-services/backend/deepiri-external-bridge-service

      - name: Type check
        run: npx tsc --noEmit
        working-directory: platform-services/backend/deepiri-external-bridge-service

      - name: Run tests
        run: npm test -- --passWithNoTests
        working-directory: platform-services/backend/deepiri-external-bridge-service

      - name: Build
        run: npm run build
        working-directory: platform-services/backend/deepiri-external-bridge-service

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./platform-services
          file: ./platform-services/backend/deepiri-external-bridge-service/Dockerfile
          push: false
          load: true
          tags: deepiri-external-bridge-service:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max
          
      - name: Verify container runs
        run: |
          docker run -d --name test-container \
            -e KAFKA_BROKERS="localhost:9092" \
            deepiri-external-bridge-service:ci
          sleep 8
          docker ps -a
          docker logs test-container
          docker inspect -f '{{.State.Running}}' test-container | grep -q true || \
            echo "::warning::Container not running — check logs above (Kafka connection failures are expected in CI)"
          docker stop test-container || true
